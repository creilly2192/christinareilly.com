import { reading } from "./now/reading";
import { listening } from "./now/listening";
import { working } from "./now/working";

//export { reading, listening, working };
export const now = { reading, listening, working };
